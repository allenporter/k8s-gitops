#cloud-config
---
install:
  device: "/dev/sda"
  # Operator needs to ensure propper boot order
  reboot: false
  auto: true

hostname: metal-{{ trunc 4 .MachineID }}
users:
- name: admin
  groups:
  - admin
  ssh_authorized_keys:
  - github:allenporter

k3s:
  enabled: true
  args:
  - --cluster-init
  env:
    K3S_TOKEN: "t0ken_g0es_h3re"
