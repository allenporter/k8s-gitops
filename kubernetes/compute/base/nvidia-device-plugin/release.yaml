---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: nvidia-device-plugin
  namespace: nvidia-device-plguin
spec:
  chart:
    spec:
      chart: nvidia-device-pugin
      version: 0.13.0
      sourceRef:
        kind: HelmRepository
        name: nvidia-device-plugin
        namespace: flux-system
  values:
    runtimeClassName: nvidia
