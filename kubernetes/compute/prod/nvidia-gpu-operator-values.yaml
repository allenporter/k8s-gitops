---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nvidia-gpu-operator
spec:
  chart:
    spec:
      chart: gpu-operator
      version: 24.9.0
  values:
    driver:
      version: 550.90.07
    devicePlugin:
      config:
        name: time-slicing-config
        default: any
