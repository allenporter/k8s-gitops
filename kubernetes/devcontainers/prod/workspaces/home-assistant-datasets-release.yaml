---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: home-assistant-datasets
  namespace: devcontainers
spec:
  releaseName: home-assistant-datasets
  chart:
    spec:
      chart: ./kubernetes/devcontainers/charts/devcontainer-workspace
      interval: 30m
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  interval: 5m
  install:
    remediation:
      retries: 3
  values:
    workspaceName: home-assistant-datasets
    git:
      url: https://github.com/allenporter/home-assistant-datasets.git
      directory: home-assistant-datasets
    storage:
      size: 10Gi
    image:
      tag: latest
    builder:
      enabled: true
      cacheFrom:
        - registry.k8s.mrv.thebends.org/devcontainers/home-assistant-ring-keypad:latest
