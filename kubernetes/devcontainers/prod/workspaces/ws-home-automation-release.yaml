---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ws-home-automation
  namespace: devcontainers
  annotations:
    devcontainer.purpose: "Home automation core, device integration SDKs (PyRainbird, Ring Keypad, Google Health API), and calendar synchronization (iCal/iCalDAV)."
spec:
  releaseName: ws-home-automation
  chart:
    spec:
      chart: ./kubernetes/devcontainers/charts/devcontainer-workspace
      interval: 30m
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  interval: 5m
  install:
    disableWait: true
    remediation:
      retries: 3
  upgrade:
    disableWait: true
  values:
    workspaceName: home-automation
    git:
      url: https://github.com/home-assistant/core.git
      directory: home-automation
    storage:
      size: 40Gi
    resources:
      requests:
        cpu: "1"
        memory: "2Gi"
      limits:
        memory: "16Gi"
    image:
      repository: registry.k8s.mrv.thebends.org/devcontainers/home-assistant-core
      tag: latest
    builder:
      enabled: true
