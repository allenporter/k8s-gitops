---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ws-{{ .Values.workspaceName }}-pvc
  namespace: {{ .Release.Namespace }}
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: {{ .Values.global.storageClass | quote }}
  resources:
    requests:
      storage: {{ .Values.storage.size | quote }}
