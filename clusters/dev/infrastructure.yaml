---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: infrastructure
  namespace: flux-system
spec:
  interval: 10m0s
  dependsOn:
    - name: crds
  sourceRef:
    kind: GitRepository
    name: flux-system
  decription:
    provider: sops
    secretRef:
      name: sops-gpg
  path: ./infrastructure/dev
  prune: true
  validation: client
