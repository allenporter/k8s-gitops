---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: hajimari
  namespace: hajimari
spec:
  chart:
    spec:
      chart: hajimari
      version: 1.1.1
  values:
    ingress:
      main:
        annotations:
          external-dns.alpha.kubernetes.io/hostname: hajimari.dev.mrv.thebends.org.
          external-dns.alpha.kubernetes.io/target: prx02.dev.mrv.thebends.org.
        hosts:
          - host: hajimari.dev.mrv.thebends.org
            paths:
              - path: /
        tls:
          - secretName: hajimari-tls
            hosts:
              - hajimari.dev.mrv.thebends.org
