---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: helm-dashboard
  namespace: helm-dashboard
spec:
  chart:
    spec:
      # renovate: registryUrl=https://helm-charts.komodor.io
      chart: helm-dashboard
      version: 0.1.3
