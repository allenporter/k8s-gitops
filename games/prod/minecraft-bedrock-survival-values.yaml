---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: minecraft-bedrock-survival
  namespace: minecraft
spec:
  releaseName: minecraft-bedrock-survival
  chart:
    spec:
      # renovate: registryUrl=https://itzg.github.io/minecraft-server-charts/
      chart: minecraft-bedrock
      version: 2.1.0
      sourceRef:
        kind: HelmRepository
        name: itzg
        namespace: flux-system
  interval: 5m
  install:
    remediation:
      retries: 3
  test:
    # Fix problem where helm fails to uninstall
    enable: false
  # Default values
  # https://github.com/itzg/minecraft-server-charts/blob/master/charts/minecraft-bedrock/values.yaml
  values:
    image:
      repository: itzg/minecraft-bedrock-server
      tag: 2023.7.1
    minecraftServer:
      serverName: "Bedrock Survival"
      eula: "TRUE"
      version: "1.19.2.02"
      ops: ${MINECRAFT_OPS}
      gameMode: survival
      memory: 2048M
      serviceType: LoadBalancer
    persistence:
      dataDir:
        enabled: true
    resources:
      requests:
        memory: 2048Mi
        cpu: 250m
      limits:
        memory: 3072Mi
    serviceAnnotations:
      external-dns.alpha.kubernetes.io/hostname: mc04.${DOMAIN}.
    rconServiceAnnotations:
      external-dns.alpha.kubernetes.io/hostname: mc04-admin.${DOMAIN}.
