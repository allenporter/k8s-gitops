---
version: '3'

tasks:
  localize:
    cmds:
      - hack/localize/localize.sh

  update-manifest:
    cmds:
      - hack/update-manifest.sh

  validate-manifest:
    cmds:
      - hack/update-manifest.sh validate
