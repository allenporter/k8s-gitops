---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kyverno
spec:
  releaseName: kyverno
  chart:
    spec:
      chart: kyverno
      sourceRef:
        kind: HelmRepository
        name: kyverno
        namespace: flux-system
  interval: 1h0m0s
  install:
    remediation:
      retries: 3
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
  # https://github.com/kyverno/kyverno/blob/main/charts/kyverno/values.yaml
  # See https://kyverno.io/docs/installation/methods/#high-availability for recommended
  # replica configuration
  values:
    replicaCount: 3
    serviceMonitor:
      enabled: true
    grafana:
      enabled: true
    backgroundController:
      replicaCount: 2
      serviceMonitor:
        enabled: true
    cleanupController:
      replicaCount: 2
    reportsController:
      replicaCount: 2
      resources:
        limits:
          # Increase default to avoid OOMs
          memory: 256Mi