---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: velero-nfs-backup-pv
  namespace: velero
spec:
  capacity:
    storage: 512Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Recycle
  storageClassName: nfs
  mountOptions:
    - hard
    - nfsvers=4.1
  nfs:
    server: sto01.prod.mrv.thebends.org
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: velero-nfs-backup-pvc
  namespace: velero
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
