---
apiVersion: infra.contrib.fluxcd.io/v1alpha1
kind: Terraform
metadata:
  name: homelab
  namespace: flux-system
spec:
  interval: 1m
  # Only detect drifts
  approvePlan: disable
  path: ./hosts/dev/
  sourceRef:
    kind: GitRepository
    name: homelab
    namespace: flux-system