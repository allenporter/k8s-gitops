name: Flux Diff
on:
  pull_request:
    branches:
      - main
    paths:
      - "**.yaml"
env:
  conf_live_branch: main
  python_version: '3.10'
jobs:
  diffs:
    name: Compute diffs
    runs-on: ubuntu-latest
<<<<<<< HEAD
    strategy:
      matrix:
        cluster_path:
          - clusters/dev
          - clusters/prod
        resource:
          - helmrelease
          - kustomization
    steps:
      - uses: allenporter/flux-local-action/diff@main
=======
    strategy_matrix:
      cluster_path:
        - clusters/dev
        - clusters/prod
      resource:
        - helmrelease
        - kustomization
    steps:
      - uses: allenporter/flux-local-action/diff@master
>>>>>>> 824b0010 (Use common github action for diffs)
        id: diff
        with:
          path: ${{ matrix.cluster_path }}
          resource: ${{ matrix.resource }}
      - name: PR Comments
        uses: mshick/add-pr-comment@v2
<<<<<<< HEAD
        if: ${{ steps.diff.outputs.diff != '' }}       
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          message-id: ${{ matrix.cluster_path }}/${{ matrix.resource }}
          message-failure: Unable to post diff
          message: |
            ```diff
            ${{ steps.diff.outputs.diff }}
=======
        if: ${{ steps.diff.diff_output != '' }}       
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          message-id: ${{ matrix.cluster_path }}/${{ matrix.resource }}
          message-failure: Unable to post kustomization diff
          message: |
            ```diff
            ${{ steps.diff.diff_output }}
>>>>>>> 824b0010 (Use common github action for diffs)
            ```