---
- hosts: kapi,kube
  become: yes
  become_user: root
  tasks:
  - name: Stop kubernetes
    service:
      name: kubelet
      state: stopped

  - name: Reset kubernetes
    command: kubeadm reset --force
