---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: external-dns
  namespace: external-dns
spec:
  valuesFrom:
  # Intended to store rfc2136.tsigSecret
  - secretKeyRef:
      name: external-dns-values
      namespace: external-dns
      key: secret-values.yaml
      optional: false
