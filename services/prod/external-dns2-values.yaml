---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: external-dns2
  namespace: external-dns
spec:
  chart:
    spec:
      chart: external-dns
      version: 1.13.1
  values:
    domainFilters: ["prod.mrv.thebends.org"]
