---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: haproxy
  namespace: haproxy
spec:
  chart:
    spec:
      # renovate: registryUrl=https://haproxytech.github.io/helm-charts
      chart: kubernetes-ingress
      version: 1.15.2
  values:
    controller:
      service:
        annotations:
          external-dns.alpha.kubernetes.io/hostname: prx02.dev.mrv.thebends.org.
        loadBalancerIP: "10.10.24.4"
      image:
        repository: haproxytech/kubernetes-ingress
        # Run latest haproxy image with http/2
        tag: 1.6.1
