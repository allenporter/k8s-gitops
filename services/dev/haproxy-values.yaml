---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: haproxy
  namespace: haproxy
spec:
  chart:
    spec:
      version: ">=1.12.3"
  values:
    controller:
      ingressClass: haproxy
      service:
        type: LoadBalancer
        annotations:
          external-dns.alpha.kubernetes.io/hostname: prx02.dev.mrv.thebends.org.
        loadBalancerIP: "10.10.24.4"
