- name: Check for networkd_dispatcher
  stat:
    path: "{{ hook_dir }}"
  register: hook_dir_path

- name: Install ifup compatibility hook
  copy:
    src: ifup-hooks.sh
    dest: "{{ hook_dir }}/50-ifup-hooks"
    mode: 0755
  when: hook_dir_path.stat.exists