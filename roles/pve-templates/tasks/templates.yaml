---
- name: Copy template scripts
  copy:
    src: create-template.sh
    dest: "{{ create_template_bin }}"
    mode: "u+x"

- name: Configure VM template
  shell:
    cmd: >
      TEMPLATE_ID={{ vm_template_id }}
      NAME={{ vm_template_name }}
      IMAGE_FILE={{ vm_template_file }}
      STORAGE={{ vm_template_storage }}
      {{ create_template_bin }}
  when: vm_template_id is defined
  register: create_template
  changed_when: "'Creating' in create_template.stdout"
<<<<<<< HEAD
=======

>>>>>>> 9a9f0e5c (Immport plays from private repo and cleanup ad-hoc plays)
