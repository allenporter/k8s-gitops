---
- name: Install etcd config / environment variables
  template:
    src: etcd.tmpl.env
    dest: /etc/default/etcd
  notify: restart etcd

- name: Install etcd service
  copy:
    src: etcd.service
    dest: /lib/systemd/system/etcd.service
  notify: restart etcd