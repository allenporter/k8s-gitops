{% set separator = joiner(',') %}
ETCDCTL_ENDPOINTS={% for host in groups['cfg'] -%}
  {{ separator() }}http://{{ host }}:2379
{%- endfor %}

KUBECONFIG="${HOME}/.env/{{ groups['kapi'][0] }}.config"
ANSIBLE_INVENTORY="${ENV_INVENTORY_ROOT}/{{ env }}/inventory.yaml"
