---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: external-dns
  namespace: external-dns
spec:
  releaseName: external-dns-dev
  chart:
    spec:
      version: ">=4.8.6"
  test:
    enable: true
  values:
    tsigKeyname: externaldns-dev-key
    domainFilters: [ "dev.mrv.thebends.org" ]
